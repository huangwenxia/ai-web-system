# Benchmark 05：已有生产页增量还原

目标页面已经接入真实 API、i18n、权限和部分交互。根据随任务提供的新定稿原型，只修指定区域的视觉与交互偏差。

保留现有 API 参数、类型、locale key、路由、权限、错误处理和未在原型变更范围内的行为。不要重写整页、删除存量 i18n、替换正确业务语义或顺手格式化无关代码。

用修改前 baseline 与修改后报告证明目标偏差已收敛且其他状态没有回归；输出最小 diff、验证矩阵和残差说明。
