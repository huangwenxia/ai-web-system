# Benchmark 01：真实数据 CRUD 列表

把随任务提供的已定稿列表页 HTML 原型落到指定 project-mamba app。使用真实生成 API/types、真实分页和 app-local i18n；保留项目要求的筛选、权限和路由行为。

要求主内容区在指定 desktop/narrow viewport、zh/en、light/dark、loading、empty、正常数据和 hover/focus 状态下逐状态还原。不要修改原型、注入 mock、手改生成 API 或把动态表体全部遮住后直接宣称完成。

完成工程验证、fidelity 机械报告、未遮罩并排检查和真实浏览器验收，并报告所有 accepted residual 与未测试状态。
