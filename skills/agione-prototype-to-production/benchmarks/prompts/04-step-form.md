# Benchmark 04：多步骤表单

把随任务提供的多步骤创建页原型落到指定 project-mamba app，并使用真实选项、预览和提交 API。先核对每一步参数契约、可返回性、校验和失败路径，再复刻视觉。

覆盖各 step、前进/后退、初始空值、字段错误、远程加载、无选项、预览、disabled、提交中和后端错误状态。禁止生成假预览数据或为贴原型跳过真实校验。

验证步骤切换的布局、选中态、focus、动效关键帧和窄屏行为，并提供三类 Gate 证据。
