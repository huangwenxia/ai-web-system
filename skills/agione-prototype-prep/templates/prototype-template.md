# REQ-YYYY-NNN 原型说明

- 版本：
- 日期：
- 状态：`Prototype Draft`
- 上游文档：`brief.md` / `solution.md` / `flow.md`

> 用途说明：
> 这份文档是"需求讨论结果"到"UI 原型生成"之间的桥梁。
> 它的目标不是重复 solution/flow 的业务逻辑，而是把已确定的逻辑**翻译成页面级、字段级的 UI 输入**。
> 生成原型时，AI 应以本文为主输入，以 `~/.claude/skills/agione-ui/SKILL.md` 为设计规范。

---

## 1. 原型目标

> 简述这个原型要验证什么、服务哪些评审场景。

## 2. 角色与视角

> 列出本原型涉及的所有角色，以及每个角色在原型中的切换方式。

| 角色 | 入口 | 与其他角色的差异 |
| --- | --- | --- |
| | | |

> 如果多角色合并到一个 HTML 文件中，说明切换方式（如顶部 Tab、下拉选择）。
> 如果拆分为多个 HTML 文件，列出文件名。

## 3. Sidebar 菜单结构

> 每个角色对应的 Sidebar 菜单，包括 Section 分组、菜单项名称、Lucide 图标和默认激活项。
> 这里定义的结构会直接映射到原型的 `<aside class="sidebar">` 区域。

### 3.1 [角色名] 菜单

| Section | 菜单项（中文） | 菜单项（English） | Lucide 图标 | 备注 |
| --- | --- | --- | --- | --- |
| | | | | |

默认激活项：`[菜单项名]`

### 3.2 [角色名] 菜单

（同上格式）

## 4. 页面清单与字段定义

> **这是本文档的核心章节。**
> 每个页面独立一个子节，包含页面目标、布局区块和每个区块的字段清单。
> AI 生成原型时应严格按照这里定义的字段展示，不应自行增减。

---

### 4.1 [页面名称]

**页面目标**：[一句话描述用户在这个页面要完成什么]
**页面类型**：`列表页（表格）` / `列表页（卡片）` / `表单页` / `向导页` / `详情页` / `Overview Dashboard` / `Shell-only`
**可见角色**：[哪些角色可以看到这个页面]

#### 布局区块

> 按从上到下的顺序列出页面的主要区块。

**PageHeader**

| 元素 | 内容 |
| --- | --- |
| 标题（中文） | |
| 标题（English） | |
| 副文本 | （可选） |
| 右侧操作按钮 | （列出按钮名和类型：Primary / Secondary / Danger） |

**KPI 卡片区**（如果有）

| 卡片名 | 主数值字段 | 数值格式 | 趋势指示 | 辅助信息 |
| --- | --- | --- | --- | --- |
| | | `¥` / `整数` / `百分比` | 是/否 | |

**FilterBar**

| 控件类型 | 字段名 | Placeholder | 宽度建议 | 备注 |
| --- | --- | --- | --- | --- |
| 搜索框 | | | 240px | |
| Select | | | 160px | 选项列表：... |

右侧主操作：`[按钮名]`（Primary / 含 `lucide:plus` 图标）

**表格 / 列表**（如果有）

| # | 列名（中文） | 列名（EN） | 数据类型 | 格式 | 可排序 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | | | `文本` / `日期` / `金额` / `Badge` / `操作` | | 是/否 | |

行操作按钮：`[按钮1]` / `[按钮2]` / `[更多 ▾]`
更多菜单内容：`[操作1]` / `[操作2（Danger）]`

**卡片网格**（如果有）

| 卡片元素 | 内容 | 位置 |
| --- | --- | --- |
| 头像/图标 | | 左上 |
| 标题 | | |
| 副标题 | | |
| 描述 | | |
| Badge | | 右上 / 底部 |
| 数据行 | | 底部 |

**分页**

- 是否需要分页：是 / 否
- 默认每页条数：

---

### 4.2 [下一个页面]

（重复 4.1 的格式）

---

## 5. 弹窗与抽屉清单

> 列出所有通过弹窗或抽屉承接的操作。
> 每个弹窗/抽屉独立一小节。

### 5.1 [弹窗名称]

- 触发方式：点击 `[按钮名]` / 行操作 `[操作名]`
- 类型：`Dialog` / `Drawer`
- 宽度建议：（Drawer 适用，600–1000px）

**表单字段**（如果是表单类弹窗）

| # | 字段名（中文） | 字段名（EN） | 控件类型 | 必填 | 校验规则 | 默认值 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | | | `Input` / `Select` / `Switch` / `DatePicker` / `TextArea` | 是/否 | | | |

**只读展示字段**（如果是详情类弹窗）

| # | 字段名 | 数据类型 | 格式 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | | | | |

**操作按钮**

- 主操作：`[按钮名]`（Primary）
- 次操作：`取消 / Cancel`（Secondary）
- 危险操作：（如有）`[按钮名]`（Danger，需二次确认）

---

## 6. Badge 与状态映射

> 列出本 REQ 中所有需要用 Badge 表达的状态值，映射到 UI Skill §2.3 的 6 套配色。

| 对象 | 状态值（中文） | 状态值（EN） | Badge 色 | 前缀符号 |
| --- | --- | --- | --- | --- |
| | | | `Green` / `Orange` / `Muted` / `Destructive` / `Blue` / `Purple` / `Yellow` | `●` / `◐` / `○` / `✕` / 无 |

## 7. 页面状态要求

> 每个页面至少覆盖以下 4 种状态。

| 页面 | 空状态文案 | 加载态方式 | 错误态处理 | 权限不足态 |
| --- | --- | --- | --- | --- |
| | 图标 + 文案 + CTA | 骨架屏 / v-loading | Toast / Inline Alert | 隐藏菜单 / 页面提示 |

## 8. Mock 数据指导

> 告诉 AI 生成原型时使用什么样的假数据，以体现真实业务场景。

### 8.1 数据量级

| 对象 | Mock 数量 | 状态分布 | 备注 |
| --- | --- | --- | --- |
| | | 例如：3 Active / 1 Pending / 1 Error | |

### 8.2 金额与数值范围

| 字段 | 数值范围 | 格式 | 币种 |
| --- | --- | --- | --- |
| | | `¥X,XXX.XX` / `X,XXX Credits` | CNY / Credit |

### 8.3 关键 Mock 人名 / 组织名

| 角色 | 名称 | 备注 |
| --- | --- | --- |
| | | |

## 9. 设计约束与评审关注点

### 9.1 设计约束

- 与已有设计系统关系：
- 需重点验证的交互：
- 业务边界不可被 UI 模糊的点：

### 9.2 评审关注点

1.
2.
3.

## 10. 原型输出说明

- 输出文件数量：`1 个总装 HTML` / `按角色拆分 N 个 HTML`
- 每个文件的角色/视角切换方式：
- 顶部 TopBar Tab 名称：

---

> **重要提醒**：
> 本文档中标记为 `[待确认]` 的项，表示上游 brief/solution/flow 中未明确定义，需要产品确认后再生成原型。
> AI 在生成原型时，对 `[待确认]` 项应使用合理的占位设计，但不应自行发明业务规则。
