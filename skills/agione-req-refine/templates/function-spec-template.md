# 功能说明书：[F001] [功能名]

- 对应需求：`REQ-YYYY-NNN`
- 研发 Owner：
- 状态：`Draft / Aligning / Final`

> 用途说明：
> 这是研发 Owner 的主文档，负责把单个功能点细化到可开发的程度。
> 业务决策和本期边界仍以 `brief.md / solution.md` 为准。

## 1. 功能概述

- 一句话描述：
- 使用频率：
- 使用角色：
- 对应业务简报 AC：

## 2. 前置条件

- 条件 1：
- 条件 2：

## 3. 主流程（Happy Path）

1. 用户在 [页面] 执行 [动作]
2. 系统校验 [条件]
3. 系统执行 [动作]
4. 返回 [结果]

## 4. 字段定义

| 字段名 | 类型 | 必填 | 长度/范围 | 校验规则 | 默认值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

## 5. 异常场景

| 编号 | 触发条件 | 系统行为 | 提示文案 |
| --- | --- | --- | --- |
| E01 |  |  |  |
| E02 |  |  |  |
| E03 |  |  |  |

## 6. 状态机

```text
初始态 -> 处理中 -> 完成
```

| 当前状态 | 动作 | 下一状态 | 谁能操作 |
| --- | --- | --- | --- |
|  |  |  |  |

## 7. 权限控制

| 角色 | 查看 | 新增 / 编辑 | 删除 | 导出 / 审批 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 8. 埋点 / 审计

| 事件 | 触发时机 | 关键参数 | 说明 |
| --- | --- | --- | --- |
|  |  |  |  |

## 9. 边界约束

- 数量上限：
- 限流规则：
- 过期时间：
- 多租户 / 多主体隔离：

## 9.1 代码地图对口（Code-Map Anchor）

> 写完字段 / 状态机 / 权限后，在这里把"具体代码落点"钉死，方便研发自查与评审者交叉对照。
> 参考：[00-overview/code-map.md](../../00-overview/code-map.md)

| 维度 | 落点 |
| --- | --- |
| 后端仓库 | 例：`metis-wanmore` |
| Maven 模块 | 例：`metis-wanmore-service/wanmore-service-web` |
| 主 Controller | 例：`TrainJobController`(`/service/train`) |
| 主 Service / 实现 | 例：`WmTrainJobService` + `K8sClient`(plugin) |
| 主 Entity / 表 | 例：`WmTrainJob`、`ResourceUsageRecord` |
| Feign 端口（新增 / 调用） | 例：调用 `metis-upms-api/RemoteUserService` |
| 跨仓库桥 | 例：无 / `CbdpRechargeController` |
| 前端 app / 视图 | 例：`apps/wanmore/src/views/manager/train/...` |
| i18n 文件 | 例：后端 `metis-wanmore-i18n/`，前端硬编码（按 CLAUDE.md） |
| 权限码 | 例：`km_train_job_create` / `km_train_job_get` |

## 10. UI 状态要求

- 空状态：
- 加载态：
- 错误态：
- 权限不足态：

## 11. 待 PM 拍板汇总

> 填写规则：
> - **问题**：一句话描述要决策什么
> - **业务背景**：解释涉及的概念是什么、为什么这里需要拍板（不能只写文档引用）
> - **选项 A / B**：每个选项的含义和业务影响，不只是标签

| # | 问题 | 业务背景（概念解释 + 为什么需要决策） | 选项 A（含义） | 选项 B（含义） | 是否阻塞开发 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  | ✅ / ❌ |

## 12. 关联文档

- 对应业务简报：
- 对应主方案：
- 对应流程文档：
