# patterns/manifests

Pattern Manifest 模板存放处。

## Pattern 类型

- `visual` - 视觉模式（如卡片样式、按钮变体）
- `layout` - 布局模式（如列表布局、表单布局）
- `interaction` - 交互模式（如拖拽、动画）

## 使用方法

```bash
node scripts/asset-tools/create-manifest.mjs --type=pattern --name=MyPattern
```
