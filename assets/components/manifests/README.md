# component manifests

这里存组件资产的 manifest 文件。

每个 manifest 建议与组件同名，例如：

- `entity-detail-card.manifest.json`
- `filter-form-panel.manifest.json`

manifest 用于记录：
- 当前状态（draft / candidate / official / project-synced）
- 成熟度等级（L0 ~ L3）
-依赖与项目兼容性
- 是否允许同步到真实项目
-评审结论与回写落点
