---
description: Decide when stable findings should be written back to skills, rules, or the real project
alwaysApply: false
---

当任务执行过程中出现稳定结论时，默认先判断它应该沉淀到哪一层：

- 任务入口判断、主链路选择、handoff、guardrails、writeback_targets -> 对应任务型主 skill 的 `SKILL.md`
- 实现约束、组件边界、状态归属、模板边界、表格模式 -> `skills/frontend-implementer-skill/SKILL.md`
- 审查口径、问题分类、排序方式、结构 / 视觉 / 体验检查点 -> `skills/page-review-skill/SKILL.md`
- 术语统一、状态文案、i18n 处理口径 -> `skills/translate-terms-skill/SKILL.md`
- 如果当前 skill 目录里已经有被 `SKILL.md` 引用的现成 `docs/` 或 `templates/`，且结论明显属于清单或模板更新，则优先改现有 skill 附属文件，不新增仓库根 `docs/`
- 只有当结论属于跨多个 skill 都默认成立的环境级约束时，才升级回写到 `rules/`
- 如果结论属于用户偏好、长期项目背景或外部参考位置，且无法从仓库直接推导，应写入 Claude memory，而不是回写 `rules/` 或 `skills/`
- 项目级实现、一次性修补、当前任务临时判断或当前会话待办 -> 真实项目、任务输出或任务列表，不回写长期协议，也不写入 Claude memory

不要把以下内容直接回写成长期协议或长期记忆：

- 一次任务的临时判断
- 强依赖某个项目上下文的特例
- 尚未验证可复用性的实现细节
- 只对单个页面成立的视觉偏好或文案偏好
- 项目结构、代码模式、目录规范、git 状态或当前任务进度这类可直接从仓库或会话推导的事实
- 未经用户明确要求的 `agione-ui-skill` 改动

如果不确定是否值得回写，至少在输出中明确标记：

- 哪些应该更新到哪个 `skills/*/SKILL.md` 或其 `docs/` / `templates/`
- 哪些才值得升级到 `rules/`
- 哪些更适合写入 Claude memory
- 哪些只是当前任务结论

参考：
@skills/existing-project-feature-skill/SKILL.md
@skills/existing-project-fix-skill/SKILL.md
@skills/frontend-implementer-skill/SKILL.md
@skills/page-review-skill/SKILL.md
@skills/translate-terms-skill/SKILL.md
