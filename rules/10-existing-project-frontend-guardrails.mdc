---
description: Project-level frontend guardrails for existing-project work
alwaysApply: true
---

本文件只定义既有项目前端开发的项目级护栏。
不定义终端同步路径；终端投影由 `scripts/sync-project-rules.mjs` 负责。
不承载完整执行流程；具体流程进入 `skills/existing-project-feature-skill/SKILL.md` 和 `skills/frontend-implementer-skill/SKILL.md`。

## 适用范围

当任务属于既有项目中的页面、组件、表单、可见 UI、局部体验或前端实现调整时，遵守以下护栏。

bug 修复、代码优化和局部体验修正还必须叠加 `rules/20-existing-project-fix-guardrails.mdc`。

## 通用护栏

- 必须优先遵循 `ai-web-system` 的项目级规则、现行 skill 协议和反馈机制；需要判断实现、审查、沉淀或回写时，优先参考 `skills/` 与 `rules/` 中的现行定义。
- 必须优先复用项目现有组件、样式体系、布局模式、数据展示模式和目录结构，不重新发明一套实现。
- 如果没有原型、只有后端接口，先补齐业务目标、字段语义、页面结构、状态边界和展示分组，再进入实现；本 rule 只要求先补齐，不展开具体产出步骤。
- 如果已有设计稿、页面稿或已确认方案，默认按既有视觉组件库和项目规范直接实现，不擅自改动已确认的信息层级和视觉主方向。
- 默认以产品级标准检查细节、对齐、节奏、状态与顺滑度；只有在用户明确要求，或当前任务已经暴露明显结构 / 视觉 / UX 风险时，才升级为独立审查流。
- 如果用户明确要求严格复查或先不要改代码，必须先按对应 skill 输出复用校验、原型对比、Vue 结构自检和自动检查结果；发现问题后再做最小修改并重跑校验。
- 页面或组件实现必须覆盖加载态、空态、错误态、权限态、禁用态等必要边界，不只覆盖正常态。
- 页面容器职责、区块职责、子组件职责要清晰，避免把页面壳逻辑下沉到通用组件里。
- 新页面 / 新功能模块按顺序闭环：先确认原型和复用，再定结构、数据归属、胶囊目录，最后实现、组装和校验。
- 表单类弹出层（新建、编辑、配置、提交等带表单的 dialog / drawer / popover）严格使用项目封装的 `FormDialog.show`；不得手写 `el-dialog` / `el-drawer` / `el-popover` + `el-form` 组合。确属非表单展示或纯确认时说明不适用；如果项目不存在 `FormDialog.show`，先阻断并说明需要补封装或请用户确认，不允许改成手写 Element Plus 表单弹层。
- 组件或页面样式严禁使用 `:global(...)` / `:global (...)` 逃逸 scoped 边界，尤其不得用它覆盖 Element Plus 内部类、浮层壳层或页面外层容器；这类写法按全局样式污染处理，必须改为组件局部类、组件 props / wrapper class、`popper-class`、`FormDialog.show` 参数或经批准的共享样式入口。
- 复杂功能按胶囊目录组织：页面根 `index.vue` 只做编排，组件私有 hook / type / constants 跟着组件走；组件胶囊入口使用 `index.vue`，不要使用 `Foo/Foo.vue`。
- 数据跟着业务组件走：不要默认用一个 `usePage` 大 hook 管全页数据；页面只保留共享状态和流程编排，纯视觉组件只接收 props。
- 数据展示、业务逻辑完整性和状态处理必须同时成立，不能只追求“页面先跑起来”。
- 新增或实质修改的前端逻辑必须添加中文注释，重点解释业务规则、状态流转、接口编排、数据转换、权限与边界判断、异步副作用和不直观兜底的意图与约束；禁止逐行复述语法或堆砌无信息量注释。简单且自解释的导入、类型、常量、直接赋值、模板和样式可不注释，未触及的历史代码无需批量补齐。

## 回写边界

- 只有形成稳定复用模式时，才考虑回写到对应 skill 协议；只有明确属于跨任务的环境级默认约束时，才升级到 `rules/`。
- 单次页面实现、单次原型补洞或单页样式偏好默认不回写。

参考：
@rules/20-existing-project-fix-guardrails.mdc
@skills/existing-project-feature-skill/SKILL.md
@skills/frontend-implementer-skill/SKILL.md
@skills/page-review-skill/SKILL.md
@skills/translate-terms-skill/SKILL.md
