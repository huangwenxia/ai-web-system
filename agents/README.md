# agents

这里存专项 Agent 的执行层设计。

## 这一层负责什么
- 定义某类专项任务由谁执行。
- 切清每个 Agent 的职责边界。
- 说明输入、输出、依赖标准和协作方式。

## 这一层不负责什么
- 不定义规则本体。
- 不成为第二主标准。
- 不替代 Skill 执行协议层。

## 与其他层的关系
- `standards`：提供规则本体。
- `commands`：提供显式任务入口。
- `skills`：提供执行协议。
- `agents`：负责角色边界与专项执行责任。
