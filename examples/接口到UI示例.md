# 接口到 UI 示例

请先读取 `standards/02-布局标准` 和 `standards/03-数据映射标准`，再根据下面接口推导页面结构。

## 输出要求
1. 页面类型判断。
2. 主区块和中间区域结构。
3. 推荐展示组件类型。
4. 需要补齐的空态、错误态、加载态。

## 适合发现的问题
- 字段分组不清。
- 主任务路径不明确。
- 展示方式与数据特性不匹配。
- 状态补齐不完整。