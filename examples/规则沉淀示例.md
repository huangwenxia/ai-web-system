# 规则沉淀示例

## 示例任务
请根据这次组件审查结果，提炼出可以长期复用的规则，并分别写入：
- `01-视觉标准`
- `02-布局标准`
- `03-数据映射标准`
- `04-组件标准`

同时说明：
1. 规则解决的问题
2. 适用场景
3. 不适用场景
4. 证据来源等级

## 示例结论
如果审查中发现：
- 多个页面都在用 `watch` 维护派生状态
- 多个卡片列表都因为没加 `min-w-0` 导致截断失效

那么前者应优先沉淀到 `04-组件标准`，后者应优先沉淀到 `01-视觉标准`。

## 价值
这个示例帮助你把“审查结果”真正转化成“标准资产”。
