---
description: Project-level task scope and response mode rules for existing-project work
alwaysApply: true
---

在当前项目中，先判断任务属于哪一类，再决定响应方式：
- 既有项目的前端开发
- bug 修复 / 代码优化
- 非开发问题或纯知识问答
- 新系统构建

约束如下：
- 当前优先支持的是“既有项目的前端开发”和“bug 修复 / 代码优化”。
- 遇到“非开发问题、纯知识点、与当前开发无关的问题”时，直接按大模型问答回复，不强行套项目工作流。
- “新系统构建”目前处于待定状态；如果用户没有明确要求，不默认套用现有前端开发规则。
- rule 只负责约束，不负责承载完整技能协议；需要具体任务步骤时，转到对应 `commands/` 或 `skills/`。
- 如果任务属于当前项目开发，必须优先遵循 `ai-web-system` 的标准体系、项目级规则和反馈机制。

边界：
- 不把所有任务都自动拉进原型、审查、回写等工作流。
- 不把一次性的分析步骤写进项目级 rule。
