---
description: Project-level task scope and response mode rules for existing-project work
alwaysApply: true
---

在当前项目中，默认先对每次对话做任务识别，再决定是否进入既有工作流。

优先按以下范围判断：
1. A-既有项目
   - 0. 产品级审查与持续优化
   - 1. 新功能模块页面 / 组件开发
     - 已有外部原型 / 设计稿 / 已确认页面
     - 尚无原型，但先通过 `agione-ui` 生成并确认原型
   - 2. 翻译、术语统一与 i18n
   - 3. bug 修复 / 代码优化
2. B-新系统构建
3. C-非开发问题 / 纯知识问答

默认约束如下：
- 每次对话先判断是否属于已知任务类型；如果属于，默认自动进入对应主工作流，不要求用户显式点名 skill。
- 当前工作流按“任务维度”分流，但执行时允许继续调用仍保留的专业型 skill；不能因为有任务型入口，就跳过专业判断和专业处理。
- A-0 是默认质量标尺：所有既有项目开发都要以产品级标准审视细节、边界、顺滑度和设计语言一致性；当用户明确要求审查 / 抠细节，或当前任务已经暴露明显结构、视觉、体验风险时，默认进入 `page-review-skill`。
- A-1 既有项目新功能开发默认进入 `existing-project-feature-skill`；实现阶段必须建立在已确认原型之上。
- A-1 如果已经有外部原型、设计稿或已确认页面，默认按既有设计方向、组件库和项目规范直接实施，不擅自改动已确认的信息层级和视觉主方向。
- A-1 如果尚无原型，不直接进入实施；必须先通过 `agione-ui` 生成并确认原型，再进入后续实现。
- 使用 `agione-ui` / `agione-ui-explore` 等 AGIOne UI 相关 skill 做页面设计或原型时，默认新建独立单文件 HTML；只有用户明确要求基于已有 HTML 文件修改、重构、优化，明确要求在原设计稿 `.html` 上继续设计，或使用 `--edit <existing.html>` 语义时，才允许直接改写已有 HTML。
- 如果用户提供旧 HTML 只是作为参考，或表达“重新设计 / 重做 / 换一种方案 / 新设计一个页面”等意图，不覆盖原文件；新建 HTML，并仅把旧文件作为理解问题和参考信息。
- 原型数值必须可解释：页面展示的每一个数值都必须来自明确业务含义本身，或由其它业务数据按清晰业务逻辑计算得出；不得为了视觉饱满、排版好看或增强氛围而随意编造数值。数据来源不明确时，应使用明确标记的占位值或记录假设，不能把随意数值当真实业务数据展示。
- 只要任务触发 UI 设计、原型设计、页面实现或视觉审查，必须先判断“目标用户是谁、当前页面任务是什么”，再决定信息层级和展示深度。设计必须服务目标用户完成判断和动作，而不是展示系统内部复杂度。
- 一切设计遵循“大道至简”。简不是内容少，而是用户判断路径短、主次清楚、动作明确。用户第一眼应能快速理解：自己正在看什么对象或范围、当前最重要的信息是什么、接下来应该关注哪里或执行什么动作、主要动作会带来什么结果。如果页面需要额外解释才能看懂，默认优先判断为过度设计或业务目标不清楚，应先收敛主任务和业务表达，而不是继续增加模块、说明、卡片、标签、指标或装饰。
- 页面可见内容只展示“当前目标用户完成当前任务所需要的信息”。严禁把与任务无关的内部实现证据暴露给用户，包括原型说明、设计备注、AI-NOTES、mock 说明、data-source、源码 API client 名称、代码变量、内部判断表达式、状态推导规则、前端路由等。
- 如果页面本身就是 API 调用、SDK 文档、日志诊断、事件追踪、运维排查等技术页面，则 API 地址、请求方法、Headers、Body、示例代码、错误码等可以展示，因为它们就是目标用户完成任务所需的信息。但即使是技术页面，也不得展示与当前任务无关的实现过程、原型备注或内部判断逻辑；敏感信息必须脱敏，低频诊断信息应降级或折叠。
- 输出前必须做“目标用户 5 秒自检”：目标用户能否在 5 秒内理解页面目的、当前状态 / 重点、下一步关注点 / 动作和动作结果？页面上是否存在与当前任务无关的代码感、接口感、内部规则感或原型说明感内容？如果未通过自检，不得交付，应先删减、改写和收敛。
- A-2 翻译类任务默认进入 `translate-terms-skill`；它同时承担术语统一、文案优化和 i18n 改造两种入口与执行职责，不混成普通实现流。
- A-3 bug 修复 / 代码优化默认进入 `existing-project-fix-skill`；再由它先查上下文、来源、调用范围、状态归属和复用影响，再编排后续修改。
- C 类问题直接按大模型问答回复，不强行套项目工作流。
- B 类问题目前不默认套用现有前端开发规则；只有用户明确要求时，再单独定义或启用对应链路。
- 如果任务属于当前项目开发，必须优先遵循 `ai-web-system` 的标准体系、项目级规则和反馈机制。
- `rules/` 只负责全局分流与环境级默认约束，不承载完整 skill 协议；需要具体任务步骤时，转到对应 `skills/`。
- `skills/` 负责可复用的任务执行协议、handoff、guardrails 和输出约束。
- 任务拆解、当前会话进度和临时待办由 task / todo 机制承接，不写进长期规则或长期记忆。
- Claude memory 只承接用户偏好、长期项目背景和外部参考位置；不承接项目结构、代码模式、目录规范、git 状态或当前任务进度。
- 能从当前仓库代码、`CLAUDE.md`、`rules/` 或 `skills/` 直接推导出的事实，不重复写入 Claude memory。

新任务维度判定：
- 如果当前对话不属于已知任务类型，继续判断它是否能形成“稳定、可复用、输入输出清晰”的新闭环任务链路。
- 只有同时满足以下条件，才提示用户考虑新增任务维度 skill：
  1. 它明确属于整个产品生产系统的一环。
  2. 它能稳定提高效率、质量或交付一致性。
  3. 它不是一次性问题，而是可复用的长期流程。
  4. 现有工作流和现存 skill 的组合无法低成本覆盖。
- 一旦发现这类候选，只能先向用户说明：它属于哪一环、解决什么问题、没有会怎样、为什么值得做；得到确认后才能新增对应任务维度 skill。
- 如果不满足这些条件，就不生成新 skill，也不新增新文档。

边界：
- 不把所有对话都自动拉进完整审查、原型、回写或治理流程。
- 不把一次性的项目特例误判成新的长期工作流。
- 不用任务维度替代专业能力维度；任务分流负责选主链路，专业 skill 负责把事情做专业。
- 不用 Claude memory 替代 rule、skill、任务列表或真实项目实现；memory 只保存跨会话仍有价值、且无法从仓库直接推导的信息。
