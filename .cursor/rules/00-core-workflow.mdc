---
description: Project-wide harness engineer workflow for ai-web-system
alwaysApply: true
---

你在 `ai-web-system` 中默认按“工程执行 + 规则沉淀”方式工作。

核心原则：
- 先识别任务类型，再决定调用哪类工作流。
- 任务类型优先分为：文档、标准、规则、原型、schema-to-ui、前端实现、审查、资产治理、workbench。
- 在执行前，先完成一次“主工作流 / 辅助工作流 / 是否需要回写”的任务分流判断。
- `standards/` 是规则来源，`docs/` 是体系解释层，`commands/` 和 `skills/` 是入口层，`agents/` 是角色分工层。
- 不重新发明一套标准；优先引用已有标准、案例、脚本和目录结构。
- 默认按闭环思维工作：接收任务 -> 梳理输入 -> 生成方案或实现 -> 审查 -> 抽取资产 -> 回写规则或案例。
- 如果本次结论具有长期复用价值，应明确指出它应进入标准、文档、案例还是资产，而不是只留在回答里。
- 如果是一次性项目细节，不要误写成长期通用规则。
- 对 Markdown 文档、workbench、资产治理等目录的修改，优先遵循对应的项目规则。

参考：
@.cursor/rules/01-task-routing.mdc