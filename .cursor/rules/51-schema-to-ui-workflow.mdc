---
description: Use when mapping backend schema or API structures to page layout and field presentation
alwaysApply: false
---

当任务属于“schema / 接口结构 -> 页面展示方案”时，优先这样做：
- 先按业务语义和字段类型分类，不要只把字段平铺到页面。
- 明确列表、详情、表单、弹窗等页面类型，再决定布局。
- 根据业务重要度决定字段优先级和展示方式。
- 补齐空态、错误态、权限态、加载态，不只覆盖正常态。
- 优先映射到现有组件模式或展示范式，找不到时再提出新模式候选。
- 该规则只负责触发与路由提醒，不在规则层重复完整 schema-to-ui 协议。

参考：
@commands/schema-to-ui.md
@skills/schema-to-ui-skill/SKILL.md
@standards/02-布局标准/README.md
@standards/03-数据映射标准/README.md
