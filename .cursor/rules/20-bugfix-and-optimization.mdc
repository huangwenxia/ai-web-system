---
description: Project-level constraints for bug fixes and code optimization in existing projects
alwaysApply: true
---

当任务属于“bug 修复”或“代码优化”时，遵守以下约束：

- 必须优先遵循 `ai-web-system` 的标准体系、项目级规则和反馈机制；需要判断修复、审查或回写时，优先参考 `standards/`、`docs/`、`rules/` 中的现行定义。
- 修改前必须先结合当前项目上下文确认影响范围，识别对应组件、公共函数、状态来源、调用方和复用范围。
- 不允许只盯当前文件做局部修补；至少要确认相关实现来自哪里、被哪些页面或模块使用。
- 优先修正问题根因，不用一次性临时补丁掩盖结构问题。
- 优化时优先保持现有行为稳定；没有证据表明需要改动的上下游逻辑，不擅自扩大修改范围。
- 修复后默认顺手检查同链路的细节一致性、交互顺滑度和回归风险，但不把每次 bug 修复都扩成大范围审查任务。
- 如果问题暴露出组件边界、状态归属、字段映射或通用函数设计缺陷，可以提出治理候选，但默认仍先完成当前修复。
- 单点 bug、局部样式修正、一次性兼容处理默认不回写规则。

参考：
@standards/04-组件标准/状态归属与数据流.md
@standards/04-组件标准/模板结构与样式边界.md
@docs/01-治理与规范/任务类型判断矩阵.md
