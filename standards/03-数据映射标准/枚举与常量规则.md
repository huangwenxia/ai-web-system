# 枚举与常量规则

## 核心规则
- 所有枚举和状态类字段应统一写入 `constant.ts` 或等价的集中常量模块
- 禁止在组件内部硬编码状态文案、颜色映射或枚举值
- 国际化项目应同步更新对应语言常量

## 展示要求
- 状态 / 枚举字段优先走统一字典组件或统一映射方法
- 枚举展示应具备未知值兜底
- 同一枚举在不同页面中的文案、颜色、顺序必须一致

## 对应来源
- `frontend.md` -> 枚举类型处理规则
