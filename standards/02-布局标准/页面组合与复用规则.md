# 页面组合与复用规则

## 页面级容器边界
- 容器组件只在页面层决策
- 子组件和内容组件默认不自带页面级容器
- 页面整体风格统一由页面层负责

## 结构对齐原则
- 列表字段应是详情字段的子集
- 创建 / 编辑字段应与详情字段尽量保持一致
- 仅允许数据来源、回填方式和交互状态存在差异

## 卡片内容区组件化
- 相同业务实体在列表、详情、编辑场景中应尽量复用同一内容区组件
- 差异通过 `mode` 或清晰的输入协议表达，不靠复制多份组件

## 对应来源
- `frontend.md` -> 容器组件使用原则
- `frontend.md` -> 复用机制
