# 页面信息层次规范

## 标准结构
原型阶段必须先明确页面的信息层次和区块职责：

1. 顶部区
身份标识，例如名称、状态、标签，以及主操作入口。

2. 次顶部
可选的上下文信息，例如描述、归属、更新时间。

3. 核心内容区
按用户决策优先级分组展示数据。

4. 底部区
可选的次要操作、分页或补充信息。

## 设计原则
- 先决定信息层次，再决定视觉样式
- 主要状态和主要操作应尽量在首屏可见
- 同一页面中的区块职责必须明确，避免重复表达相同信息

## 对应来源
- `skills/agione-ui-skill/SKILL.md` -> 页面信息层次与区块职责
