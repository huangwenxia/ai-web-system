# 数据填充原则

## 设计和原型阶段
- 使用有业务语义的真实感数据
- 不使用 `xxx`、`123` 这类无语义占位符
- 名称、时间、状态、数值应体现真实业务差异
- 同一页面至少覆盖 3 到 5 行数据样本
- 样本应尽量覆盖正常、警告、禁用等不同状态

## 空态
- 空态需要说明原因
- 空态最好包含引导动作
- 空态不是“没有数据”的简单留白

## 对应来源
- `页面设计.md` -> 数据填充原则
