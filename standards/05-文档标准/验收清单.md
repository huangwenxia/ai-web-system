# 验收清单

## 页面级
- 页面信息层次是否清晰
- 主状态和主操作是否首屏可见
- 列表 / 详情 / 创建 / 编辑结构是否对齐
- 加载、空态、错误、禁用状态是否完整

## 组件级
- 输入输出协议是否清晰
- 是否存在镜像状态或边界不清的副作用
- 是否符合视觉标准和布局标准
- 是否已有组件文档和示例

## 数据映射
- 字段类型是否已分类
- 展示方式、交互方式、异常态是否统一
- 枚举和常量是否集中维护
