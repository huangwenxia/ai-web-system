# 组件 API 设计

## 核心规则
- `props` 类型完整、默认值明确，只暴露必要输入
- 事件名表达用户意图或状态变化，不使用实现导向命名
- 双向绑定优先使用标准 `modelValue / update:modelValue` 或具名 `v-model`
- `slots` 用于结构扩展，不用于绕过组件边界
- `defineExpose` 只暴露必要实例能力

## 审查目标
组件的输入、输出、扩展点必须足够清晰，使用者不看实现也能正确使用。

## 对应来源
- `frontend.md` -> 组件 API 设计
