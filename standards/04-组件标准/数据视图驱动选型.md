# 数据视图驱动选型

## 选型原则
- 先对字段进行语义分类，再映射组件
- 组件优先级：项目组件 > 设计系统组件 > 基础组件库
- 找不到合适组件时，先回退到明确的元数据展示，不强行拼装错误组件

## 推荐承接方向
- 页面主内容容器、卡片容器、详情头部、Tab、步骤页、滚动区域等，优先使用项目级布局组件
- 数值展示、状态展示、图标和操作区，优先使用项目中已有成熟组件
- 列表、详情、编辑三种场景尽量复用同一业务内容区组件

## 对应来源
- `frontend.md` -> 数据视图驱动开发
- `frontend.md` -> 数据分类 → 组件映射
